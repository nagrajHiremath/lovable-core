spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/nagrajHiremath/lovable-config

      username: nagrajHiremath
      password: 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
      default-label: main

server.port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/nagrajHiremath/lovable-config
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false